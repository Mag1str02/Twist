#pragma once

#include "../simulator.hpp"

#include <twist/rt/sim/scheduler/pct/scheduler.hpp>

#include <optional>

namespace twist::sim::sched {

using PctScheduler = twist::rt::sim::system::scheduler::pct::Scheduler;

}  // namespace twist::sim::sched
