#pragma once

/*
 * // [0, alts)
 * size_t twist::test::Choice(alts);
 *
 * // [lo, hi)
 * size_t twist::test::Choice(lo, hi);
 *
 */

#include <twist/rt/cap/test/choice.hpp>

namespace twist::test {

using twist::rt::cap::test::Choice;

}  // namespace twist::test
